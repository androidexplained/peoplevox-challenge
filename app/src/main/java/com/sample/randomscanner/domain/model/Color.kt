package com.sample.randomscanner.domain.model

import android.graphics.Color

enum class BarcodeColor(val resource: Int) {
    Yellow(Color.YELLOW), Green(Color.GREEN), Blue(Color.BLUE), Red(Color.RED), Black(Color.BLACK),
    White(Color.WHITE)
}